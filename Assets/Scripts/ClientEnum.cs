

namespace ClientEnum
{
    public enum Goods
    {
        Gold,
        Ruby,
    }

    public enum LoginType
    {
        None,
        Google,
        Guest,
    }

    public enum CharacterType
    {
        Player,
        Enemy
    }

    public enum ObjectType
    {
        Object,
        UI
    }
}